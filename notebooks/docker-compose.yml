version: '3.8'

services:
  falkordb:
    image: falkordb/falkordb:latest
    container_name: grawiki-falkordb
    ports:
      - "6379:6379"
      - "3000:3000"
    volumes:
      - falkordb-data:/data
    restart: unless-stopped

volumes:
  falkordb-data:
    driver: local
