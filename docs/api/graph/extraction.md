# Extraction

This page covers the advanced extraction layer that turns raw text into a graph-shaped intermediate representation before persistence. Most users should interact with extraction through [`GraphRAG`][grawiki.rag.graph_rag.GraphRAG] rather than constructing these pieces directly.

::: grawiki.graph.extraction
