# FalkorDB Adapter

[`FalkorGraphDB`][grawiki.db.falkordb.FalkorGraphDB] is the current concrete backend. It is documented as an advanced page because the adapter exposes both stable `GraphDB` methods and backend-specific operational details such as embedded-process management and lower-level query helpers.

::: grawiki.db.falkordb.FalkorGraphDB
