# Keyword Path Retriever

[`KeywordsPathRetriever`][grawiki.retrieval.keywords.KeywordsPathRetriever] expands a query into key phrases, retrieves matching entity seeds, and synthesizes one-hop graph context into the returned node content. This makes graph neighborhoods directly usable by downstream prompting without exposing low-level traversal code first.

::: grawiki.retrieval.keywords
