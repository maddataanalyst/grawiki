version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  apt_packages:
    - pandoc

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: true

python:
  install:
    - method: uv
      command: pip
      path: .
      extras:
        - docs
